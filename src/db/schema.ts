/**
 * PostgreSQL schema (Drizzle). Add `pgTable` definitions here.
 * Then run `npm run db:push` (dev) or `npm run db:generate` + `npm run db:migrate`.
 *
 * This starter ships with no tables — the database stays empty until you define some.
 */

export {};
